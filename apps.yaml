apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
spec:
  destination:
    name: https://kubernetes.default.svc
    namespace: argocd
  source:
    path: apps
    repoURL: 'https://github.com/napestershine/gitops-argocd'
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true